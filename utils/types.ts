export enum JobStatus {
  Pending = "pending",
  Interview = "interview",
  Offer = "offer",
  Declined = "declined",
}

export enum JobType {
  FullTime = "full-time",
  Internship = "internship",
}

export enum JobMode {
  Office = "office",
  Hybrid = "hybrid",
  Remote = "remote",
}
